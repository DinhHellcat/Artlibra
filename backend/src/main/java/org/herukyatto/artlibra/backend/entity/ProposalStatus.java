package org.herukyatto.artlibra.backend.entity;

public enum ProposalStatus {
    PENDING,  // Đang chờ client xem xét
    ACCEPTED, // Đã được client chấp nhận
    REJECTED  // Đã bị client từ chối
}